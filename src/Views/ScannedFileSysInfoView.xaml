<UserControl x:Class="FattyScanner.Views.ScannedFileSysInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FattyScanner.Views"
             xmlns:vm="clr-namespace:FattyScanner.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="200"
             d:DataContext="{d:DesignData Type=vm:ScannedFileSysInfoViewModel, IsDesignTimeCreatable=False}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Rectangle Margin="1"/>
        <ListBox Grid.Row="1"></ListBox>
    </Grid>
</UserControl>
